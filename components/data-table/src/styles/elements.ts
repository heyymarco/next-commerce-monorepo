// elements:
export const theadElm = ':where(.thead)' // one degree specificity to overwrite <DataTableHeader> component
export const tfootElm = ':where(.tfoot)' // one degree specificity to overwrite <DataTableFooter> component
export const tbodyElm = ':where(.tbody)' // one degree specificity to overwrite <DataTableBody>   component

export const trElm    = ':where(.tr)'    // one degree specificity to overwrite <tr>              component
export const tdElm    = ':where(.td)'    // one degree specificity to overwrite <td>              component
export const thElm    = ':where(.th)'    // one degree specificity to overwrite <th>              component
