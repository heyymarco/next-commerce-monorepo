// elements:
export const theadElm = '.thead' // one degree specificity to overwrite <DataTableHeader> component
export const tfootElm = '.tfoot' // one degree specificity to overwrite <DataTableFooter> component
export const tbodyElm = '.tbody' // one degree specificity to overwrite <DataTableBody>   component

export const trElm    = '.tr'    // one degree specificity to overwrite <tr>              component
export const tdElm    = '.td'    // one degree specificity to overwrite <td>              component
export const thElm    = '.th'    // one degree specificity to overwrite <th>              component
