// internals:
import type {
    // types:
    AuthConfigClient,
}                           from './types.js'
import {
    // types:
    defaultAuthConfigShared,
}                           from './auth.config.shared.js'



export const defaultAuthConfigClient : AuthConfigClient = defaultAuthConfigShared;
